# General Configuration Options
ci_runner: github

# Configure vulnerability scanning.
vulnerabilities:
  fatal_cvss_score: 9.2  # Configure the maximum allowable CVSS score.

# Configure secrets scanning.
secrets:
    paths:         # Configure paths to scan for secrets.
        - /%%
